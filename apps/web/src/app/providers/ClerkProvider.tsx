import type { PropsWithChildren, ReactElement } from "react";

export default function ClerkProvider({
  children,
}: PropsWithChildren): ReactElement {
  // Stub: auth real se integrará fuera de la Entrega 1
  return <>{children}</>;
}
