import type { PropsWithChildren, ReactElement } from "react";

export default function QueryClientProvider({
  children,
}: PropsWithChildren): ReactElement {
  // Stub: react-query real se integrará fuera de la Entrega 1
  return <>{children}</>;
}
